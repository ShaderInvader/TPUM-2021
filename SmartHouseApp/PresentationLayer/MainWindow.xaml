<Window x:Class="PresentationLayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationLayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Border Background="Beige"/>
        <Border Background="Chocolate" Grid.Column="1"/>
        <Border Background="LightPink" Grid.Column="2"/>
        <Label Content="Hello World!" HorizontalAlignment="Left" Margin="29,22,0,0" VerticalAlignment="Top" Height="33" Width="106"/>
        <Button Content="Butą" HorizontalAlignment="Left" Margin="82,325,0,0" VerticalAlignment="Top" Command="{Binding Command}"/>
        <Label x:Name="label1" Content="{Binding Name, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Column="1"/>
    </Grid>
</Window>
